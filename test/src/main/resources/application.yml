security:
  basic:
    enabled: false

spring:
  data:
    mongodb:
      uri: ${MONBODB_URI:mongodb://mongodb:27017/sailfish}
      repositories.enabled: false
    redis:
      repositories.enabled: false
  redis:
    host: localhost
    port: 6379


